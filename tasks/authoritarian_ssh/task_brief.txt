Issue:
Our deployment pipeline has been demoted from “trusted automation” to “uninvited guest.” The server is rejecting the deploy user’s SSH attempts like a strict bureaucrat who just discovered rules.

The key hasn’t changed. The pipeline hasn’t changed. But the server has decided that access is now a privilege, not a right.

Required Steps:

Identify why the deploy user’s key is no longer considered worthy.
Restore access using the existing key (no cheating with new ones).
Do not modify /etc/ssh/sshd_config — this is not a dictatorship reform exercise.
Do not weaken SSH security to “just make it work.”
When the pipeline can SSH successfully again, execute:
"submit"
